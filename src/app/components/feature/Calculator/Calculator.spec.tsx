import React from 'react';
// @ts-ignore
import { expect } from 'chai';
import Calculator from './Calculator';

describe('Calculator', function CalculatorSpec () {
    it('it exists', () => {
        expect(Calculator).to.be.ok;
    });
});
